**One of these is required**
SCAN_PATH="/mnt/dir /mnt/dir2" #Space-delimited dirs as mounted inside the container
SCAN_FILE="/mnt/dir/paths.txt" #Text file that is newline-delimited of dirs as mounted inside the container, OVERRIDES SCAN_PATH

**Required**
LOG_PATH
QUAR_PATH

**Optional**
PROXY_SERVER
PROXY_PORT
DEF_UPD_FREQ
ENABLE_SSH=anything
MODE=[av,find] (Default is both)

VPSA_ACCESSKY=[VPSA user accesskey]
VPSA_IP=[Optional, will determine on own]

FIND_CRON="* * * * *"
AV_CRON="* * * * *"
